#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */

int main(void)
{
       int number;

	for (number = '0'; number <= '10'; number++)
	{
		printf("%d\n");
	}

	return (0);
}
